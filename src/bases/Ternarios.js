let firstname;
let lastname;

//console.log(`${firstname || 'no firstname'} ${lastname || 'no lastname'}`);

const isActive = true;

const message = isActive ? 'active' : 'inactive';

console.log(message);